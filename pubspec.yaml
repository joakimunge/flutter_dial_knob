name: knob
description: 'A customizable, rotatable knob widget for Flutter.'
version: 0.0.1
repository: 'https://github.com/joakimunge/flutter_knob'

environment:
  sdk: ^3.5.0
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
